public enum OutboundPrivacyPolicy {
    /// The leaderboard toggle governs both writes and reads to whoburnedmore.
    public static func allowsLeaderboardNetwork(syncEnabled: Bool) -> Bool {
        syncEnabled
    }
}
